import javafx.geometry.Bounds;
import javafx.scene.Group;
import javafx.scene.Scene;
import javafx.scene.canvas.Canvas;
import javafx.scene.canvas.GraphicsContext;
import javafx.scene.paint.Color;
import javafx.scene.shape.ArcType;
import javafx.scene.shape.Circle;
import javafx.scene.shape.Rectangle;
import javafx.scene.shape.Shape;
import javafx.stage.Stage;
/*
public class Player extends Circle implements Constants
{
    private GraphicsContext graphicsContext;
    private int xPos = (gameWidth + insets) / 2;
    private int yPos = (gameHeight + insets) / 2;
    //private Rectangle playerHitbox = new Rectangle(xPos, yPos, circleW, circleH);

    public Player(GraphicsContext gc)
    {
        graphicsContext = gc;
    }


    public void incrementPlayerX(int x)
    {
        xPos += x;
    }

    public void incrementPlayerY(int x)
    {
        yPos += x;
    }

    public void drawPlayer()
    {
        graphicsContext.setFill(Color.BLACK);
        graphicsContext.fillOval(xPos, yPos, circleW, circleH);
    }

    public void setXPos(int x)
    {
        xPos = x;
    }

    public void setYPos(int y)
    {
        yPos = y;
    }

    public int getYPos()
    {
        return yPos;
    }

    public int getXPos()
    {
        return xPos;
    }



}
*/